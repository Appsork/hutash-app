# Proven launch: the shared hutash_inference server via uvicorn --factory.
command: uvicorn
args:
- --factory
- hutash_inference.server:create_app
- --host
- 127.0.0.1
- --port
- '{port}'
env:
  HUTASH_MODEL_ID: kokoro
  HUTASH_MODEL_DIR: '{model_dir}'
  HF_HUB_CACHE: '{weights_dir}'
  HF_HUB_OFFLINE: '1'
  HUTASH_HF_REVISION: f3ff3571791e39611d31c381e3a41a3af07b4987
port: 8000
health_endpoint: /health
health_timeout: 120
