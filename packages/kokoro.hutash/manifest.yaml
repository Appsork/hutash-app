# Identity card — engine reads this first. runtime: venv
hutash_format: '1.0'
id: kokoro
name: Kokoro
version: 1.0.2
type: model
runtime: venv
gpu: none
license: Apache-2.0
capabilities:
- id: tts
  modality: tts
platforms:
- windows
- linux
description: Natural-sounding text-to-speech with multiple voice presets and speaking
  styles.
