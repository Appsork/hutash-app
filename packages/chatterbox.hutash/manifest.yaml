# Identity card — engine reads this first. runtime: venv
hutash_format: '1.0'
id: chatterbox
name: Chatterbox
version: 1.0.2
type: model
runtime: venv
gpu: optional
license: MIT
capabilities:
- id: voice-clone
  modality: clone
- id: tts
  modality: clone
platforms:
- windows
- linux
description: Voice cloning from a 5–30 second reference audio clip.
